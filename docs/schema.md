# Timeline schema

`timeline.json` is the only input the composer and the verifier need. This is
the JSON Schema (draft 2020-12) it is validated against; the fields are
explained, one by one, in the [reference](reference.md).

```json
--8<-- "src/screencast/schema/timeline.schema.json"
```
